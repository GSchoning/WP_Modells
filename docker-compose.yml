# docker-compose.yml — local development for the Precipice POC.
#
# Usage:
#   docker compose run --rm app                       # run the pipeline
#   docker compose run --rm app bash                  # shell inside the container
#   docker compose run --rm --service-ports notebook  # JupyterLab on :8888

services:
  app:
    build: .
    image: precipice-poc:latest
    volumes:
      # User-supplied inputs (the repo uses ./Data with a capital D).
      - ./Data:/app/Data
      # Outputs and reports — writable from inside the container.
      - ./outputs:/app/outputs
      - ./reports:/app/reports
      # Live source mount so edits on the host take effect without rebuilds.
      - ./src:/app/src
      - ./config.yaml:/app/config.yaml:ro
      - ./config_hutton.yaml:/app/config_hutton.yaml:ro
      - ./config_gubberamunda.yaml:/app/config_gubberamunda.yaml:ro
    environment:
      - PYTHONUNBUFFERED=1
      # The API bootstraps every module whose config exists (precipice,
      # hutton, gubberamunda) — each runs its MF6 baseline on first start.
      # Limit for a faster boot, e.g.: GABORA_AQUIFERS=precipice
      - GABORA_AQUIFERS

  api:
    extends:
      service: app
    command: python -m src.cli serve --config config.yaml
    ports:
      - "8000:8000"
    volumes:
      - ./frontend:/app/frontend
      # Decision audit trail — durable runtime data, survives redeploys.
      - ./var:/app/var

  notebook:
    extends:
      service: app
    command: >
      jupyter lab
        --ip=0.0.0.0
        --port=8888
        --no-browser
        --ServerApp.token=''
        --ServerApp.password=''
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/app/notebooks
